package ca.ulaval.glo4002.booking.domain.passOrdering.passes.passTypes;

import ca.ulaval.glo4002.booking.domain.passOrdering.passes.Pass;

public abstract class PackagePass extends Pass {

    // TODO receive services here
    protected PackagePass() {
        // TODO call OxygenService.reserve(festival.allDates)
        // TODO call TransportService.reserve(Festival.startDate)
        // TODO call TransportService.reserve(Festival.endDate)
    }
}
